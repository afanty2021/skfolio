[根目录](../../../CLAUDE.md) > [src](../../) > [skfolio](../) > **distance**

# distance - 距离度量模块

## 模块职责

distance 模块提供了各种距离和相似性度量方法，用于量化资产之间的关系。这些距离度量被广泛应用于聚类分析、资产选择、风险管理和投资组合优化，帮助理解资产间的依赖结构和相似性。

## 模块结构

```
distance/
├── __init__.py              # 模块入口
├── _base.py                # 基础类
└── _distance.py            # 距离度量实现
```

## 入口与启动

```python
from skfolio.distance import (
    mutual_info,
    pearson_distance,
    spearman_distance,
    kendall_distance,
    variation_of_information,
)
```

## 对外接口

### 距离度量函数

```python
# 基于相关性的距离
pearson_dist = pearson_distance(X)      # 1 - |correlation|
spearman_dist = spearman_distance(X)    # 1 - |spearman|
kendall_dist = kendall_distance(X)      # 1 - |kendall|

# 信息论距离
mi = mutual_info(X, Y)                  # 互信息
vi = variation_of_information(X, Y)     # 变分信息
```

## 主要距离类型

### 1. 线性相关性距离
- **Pearson 距离**: `1 - |correlation|`
- **Spearman 距离**: 基于秩相关
- **Kendall 距离**: 基于 concordant pairs

### 2. 信息论距离
- **互信息**: 衡量非线性依赖
- **变分信息**: 信息论的聚类度量
- **KL 散度**: 相对熵

### 3. 其他距离
- **欧氏距离**: 标准几何距离
- **马氏距离**: 考虑协方差结构

## 关键依赖

### 核心依赖
- **numpy**: 数值计算
- **scipy**: 统计函数
- **scikit-learn**: 度量工具

## 测试与质量

### 测试文件
- `tests/test_distance/test_distance.py` - 距离函数测试

## 变更记录

### 2025-12-09 06:15:32 UTC - 模块初始化
- 📚 **创建模块文档**：记录了 distance 模块功能

---

## 使用建议

1. **聚类分析**：选择合适的距离度量进行资产聚类
2. **非线性关系**：使用互信息捕捉非线性依赖
3. **稳健性**：使用 Spearman 或 Kendall 距离处理异常值
4. **组合使用**：结合多种距离获得全面认识