[根目录](../../../CLAUDE.md) > [src](../../) > [skfolio](../) > **datasets**

# datasets - 数据集模块

## 模块职责

datasets 模块提供了预加载的金融数据集，包括股票价格、因子数据、宏观经济数据等。这些数据集用于示例、教学、测试和基准测试，让用户可以快速开始使用 skfolio 而无需寻找和准备数据。

## 模块结构

```
datasets/
├── __init__.py              # 模块入口
├── _base.py                # 数据集基类
└── data/                   # 数据文件存放目录
    └── __init__.py
```

## 入口与启动

```python
from skfolio.datasets import (
    load_sp500_dataset,
    load_factors_dataset,
    load_prices_dataset,
    load_sp500_implied_vol_dataset,
)
```

## 对外接口

### load_sp500_dataset

加载 S&P 500 股票价格数据：

```python
from skfolio.datasets import load_sp500_dataset

prices = load_sp500_dataset()
print(f"数据形状: {prices.shape}")
print(f"时间范围: {prices.index[0]} 到 {prices.index[-1]}")
print(f"包含股票: {prices.columns.tolist()[:10]}...")  # 显示前10个
```

### load_factors_dataset

加载因子数据：

```python
factors = load_factors_dataset()
# 通常包含市场因子、规模因子、价值因子等
```

### load_prices_dataset

通用价格数据加载器：

```python
prices = load_prices_dataset(
    dataset_name="crypto",    # 数据集名称
    frequency="daily",        # 频率
    start_date="2020-01-01",  # 开始日期
)
```

### load_sp500_implied_vol_dataset

加载 S&P 500 隐含波动率数据：

```python
implied_vol = load_sp500_implied_vol_dataset()
# 用于 Black-Litterman 和其他需要隐含波动率的模型
```

## 关键依赖

### 核心依赖
- **pandas**: 数据处理
- **requests**: 下载数据（如果需要）
- **boto3**: AWS S3 访问（如果数据存储在云端）

## 数据集说明

### 1. S&P 500 数据集
- **时间范围**：通常为 10-20 年的日数据
- **包含股票**：精选的 S&P 500 成分股
- **数据频率**：日度
- **数据质量**：已清洗，处理了分割、股息等

### 2. 因子数据集
- **Fama-French 三因子**：市场、规模、价值
- **动量因子**：价格动量
- **质量因子**：盈利质量等

## 测试与质量

### 测试文件
- `tests/test_dataset/test_dataset.py` - 数据集测试

### 测试覆盖
- ✅ 数据加载功能
- ✅ 数据完整性检查
- ✅ 数据格式验证
- ✅ 缓存机制

## 常见问题 (FAQ)

### Q: 数据更新频率？
A: 大部分数据集定期更新，但主要用于演示目的。

### Q: 可以使用自己的数据吗？
A: 是的，skfolio 接受 pandas DataFrame 格式的数据。

### Q: 数据存储在哪里？
A: 首次加载后会缓存到本地，加速后续访问。

## 相关文件清单

### 核心实现
- `_base.py` - 数据加载基类

### 示例文件
- 所有示例文件都使用这些数据集

## 变更记录

### 2025-12-09 06:15:32 UTC - 模块初始化
- 📚 **创建模块文档**：记录了 datasets 模块功能
- ✅ **数据集列表**：说明了可用的预置数据集
- 📝 **使用示例**：提供了数据加载的基本用法

---

## 使用建议

1. **学习用途**：这些数据集非常适合学习 skfolio
2. **快速原型**：快速验证想法和算法
3. **基准测试**：使用标准数据集比较不同方法
4. **教学材料**：用于教学和演示

## 扩展开发

```python
def load_custom_dataset(name: str, **kwargs):
    """加载自定义数据集"""
    # 实现自定义数据加载逻辑
    pass
```