[根目录](../../../CLAUDE.md) > [src](../../) > [skfolio](../) > **cluster**

# cluster - 资产聚类模块

## 模块职责

cluster 模块提供了资产聚类算法，用于识别资产间的相似性和相关性结构。聚类结果广泛应用于分层风险平分（HRP）、分层等风险贡献（HERC）等投资组合优化方法，帮助构建更分散和稳健的投资组合。

## 模块结构

```
cluster/
├── __init__.py              # 模块入口
└── _hierarchical.py         # 层次聚类实现
```

## 入口与启动

```python
from skfolio.cluster import HierarchicalClustering
```

## 对外接口

### HierarchicalClustering

层次聚类实现：

```python
clusterer = HierarchicalClustering(
    linkage_method="ward",           # 链接方法
    distance_metric="euclidean",     # 距离度量
    n_clusters=None,                 # 聚类数量
)

clusterer.fit(correlation_matrix)
labels = clusterer.labels_
linkage_matrix = clusterer.linkage_matrix_
```

## 关键功能

### 1. 链接方法
- **ward**: 最小化簇内方差
- **complete**: 最大距离
- **average**: 平均距离
- **single**: 最小距离

### 2. 距离度量
- **euclidean**: 欧氏距离
- **correlation**: 相关性距离
- **mutual_info**: 互信息

## 关键依赖

### 核心依赖
- **numpy**: 数值计算
- **scipy**: 层次聚类实现
- **scikit-learn**: 基础聚类接口

## 测试与质量

### 测试文件
- `tests/test_cluster/test_hierarchical.py` - 层次聚类测试

## 变更记录

### 2025-12-09 06:15:32 UTC - 模块初始化
- 📚 **创建模块文档**：记录了 cluster 模块功能

---

## 使用建议

1. **HRP/HERC**：作为分层风险平分优化的前置步骤
2. **资产分组**：识别相似资产群组
3. **分散化**：通过跨聚类构建分散化组合
4. **可视化**：使用树状图展示资产关系